# Generated by generator.py from ../resnet18.onnx. Both are large and reproducible, so
# they are not committed; include/resnet18_graph.h is, since it is the interface the test
# compiles against.
resnet18_data.bin
layers/
