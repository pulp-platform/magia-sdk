# Generated by generator.py from ../mobilevitv2.onnx. Both are large and reproducible, so
# they are not committed; include/mobilevit_graph.h is, since it is the interface the test
# compiles against.
mobilevit_data.bin
layers/
