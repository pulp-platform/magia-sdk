# Generated by generator.py from ../tiny_vit_5m.onnx. Both are large and reproducible, so
# they are not committed; include/tinyvit_graph.h is, since it is the interface the test
# compiles against.
tinyvit_data.bin
layers/
